<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.chat4j.platform.mapper.MessageMapper">

    <resultMap type="Message" id="MessageResult">
        <result property="msgId"    column="msg_id"    />
        <result property="tempMsgId"    column="temp_msg_id"    />
        <result property="userId"    column="user_id"    />
        <result property="userInfo"    column="user_info" typeHandler="com.abc.chat4j.platform.domain.entity.typehandler.MessageUserInfoTypeHandler" />
        <result property="type"    column="type"    />
        <result property="content"    column="content"    />
        <result property="roomId"    column="room_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
        <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectMessageVo">
        select msg_id, temp_msg_id, user_id, user_info, type, content, room_id, create_time, update_time, status, ver from tb_message
    </sql>

    <select id="selectMessageList" resultMap="MessageResult">
        select msg_id, temp_msg_id, user_id, user_info, type, room_id, content, create_time, status
        from tb_message
        <where>
            <if test="userId != null">and user_id = #{userId}</if>
            <if test="minUpdateTime != null">and update_time >= #{minUpdateTime}</if>
        </where>
    </select>

</mapper>
