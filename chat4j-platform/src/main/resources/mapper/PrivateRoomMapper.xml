<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.chat4j.platform.mapper.PrivateRoomMapper">

    <resultMap type="PrivateRoom" id="PrivateRoomResult">
        <result property="privateRoomId"    column="private_room_id"    />
        <result property="roomId"    column="room_id"    />
        <result property="userId"    column="user_id"    />
        <result property="friendId"    column="friend_id"    />
        <result property="roomKey"    column="room_key"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
        <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectPrivateRoomVo">
        select private_room_id, room_id, user_id, friend_id, room_key, create_time, update_time, status, ver from tb_private_room
    </sql>
    <select id="selectByRoomId" resultMap="PrivateRoomResult">
        select * from tb_private_room where room_id = #{roomId}
    </select>
    <select id="selectPrivateRoomListByRoomIds" resultMap="PrivateRoomResult">
        select * from tb_private_room where room_id in
        <foreach collection="roomIds" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>

</mapper>
